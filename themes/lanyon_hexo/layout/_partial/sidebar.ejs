<!-- PARTIAL: SIDEBAR  -->
    <!-- Target for toggling the sidebar `.sidebar-checkbox` is for regular
         styles, `#sidebar-checkbox` for behavior. -->
    <input type="checkbox" class="sidebar-checkbox" id="sidebar-checkbox">

    <!-- Toggleable sidebar -->
    <div class="sidebar" id="sidebar">
      <div class="sidebar-item">
        <!--TODO: Make programmatic -->
         <h3 class="masthead-title">
             <!--TODO: programmatically-->
           <!--  <a href="{{ site.baseurl }}/" title="Home">{{ site.title }}</a>
            <small>{{ site.tagline }}</small> -->
             <%= _.capitalize(config.title) %>
            <small> <%= _.capitalize(config.subtitle) %></small>
            <!--  -->
          </h3>

        <p> <%= _.capitalize(config.description) %> </p>
      </div>

      <nav class="sidebar-nav">

        <!--TODO: fix this  -->
        <!-- <a class="sidebar-nav-item{% if page.url == site.baseurl %} active{% endif %}" href="{{ site.baseurl }}/">Home</a> -->

        <a class="sidebar-nav-item " href="/">Home</a>
		
		<!-- adding pages programmatically, pages are found in source folder and are .md files https://hexo.io/docs/writing.html -->
		
		<% site.pages.each(function(page){%>
		 <a class="sidebar-nav-item " href="<%= page.path%>"><%= _.capitalize(page.title) %></a>
		<% })%>

		
		<% for(var k in site.categories.data){ %>
		 <a class="sidebar-nav-item " href="/categories/<%= site.categories.data[k].name%>"><%= _.capitalize(site.categories.data[k].name) %></a>
		<% } %>

    <!-- If you want to display the tags  -->

  <!--   <% for(var k in site.tags.data){ %>
     <a class="sidebar-nav-item " href="/"><%= site.tags.data[k].name %></a>
    <% } %>
 -->

        <!--TODO: add programmatic generation of pages  -->
        <!-- The code below dynamically generates a sidebar nav of pages with
          `layout: page` in the front-matter. See readme for usage. -->

       <!--     {% assign pages_list = site.pages | sort:"url" %}
        {% for node in pages_list %}
          {% if node.title != null %}
            {% if node.layout == "page" %}
              <a class="sidebar-nav-item{% if page.url == node.url %} active{% endif %}" href="{{ node.url }}">{{ node.title }}</a>
            {% endif %}
          {% endif %}
        {% endfor %} -->

        <!-- Temporary place holder -->
    <!--     <a class="sidebar-nav-item active" href="/">Example Page</a>
         <a class="sidebar-nav-item " href="/">Example Page 2</a> -->

        <!--   <a class="sidebar-nav-item" href="{{ site.github.repo }}/archive/v{{ site.version }}.zip">Download</a> -->

        <!-- Can link to URLs in config -->
        <!-- <a class="sidebar-nav-item" href="{{ site.github.repo }}">GitHub project</a>
        <span class="sidebar-nav-item">Currently v{{ site.version }}</span> -->

        <!--TODO: programmatic add some links from config abotu social media stuff  -->

       <a class="sidebar-nav-item " href="#"  >&nbsp; </a>



      <% config.contact.forEach(function(c){ %>
  
        <% if(c.email !== undefined ){ %>
    
         <a class="sidebar-nav-item " href="mailto:<%= c.email %>"title="Hey <%= config.author %>" >@ Email</a>

        <% }else if(c.github !==undefined ){%>
           <a class="sidebar-nav-item icon-github" href="https://github.com/<%= c.github %>"  target="_blank">Github</a>
        <% }else if(c.twitter !==undefined ){%>
           <a class="sidebar-nav-item " href="https://twitter.com/<%= c.twitter %>"  target="_blank">Twitter </a>
        <% }%>

      <%}) %>

         
      </nav>

      <div class="sidebar-item">
        <p>
         <!--  &copy; {{ site.time | date: '%Y' }}. All rights reserved. -->
          Copyright notice
        </p>
      </div>
    </div>
  <!--PARTIAL: END SIDEBAR  